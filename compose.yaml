services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=energ_orders'
      - 'MYSQL_PASSWORD=admin'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=admin'
    ports:
      - '3306'
